*.db
*.pyc
__pycache__/
.env
.pytest_cache/
dist/
*.egg-info/
.claude
.mcp.json

# Sample API responses (local fixtures, not part of the app)
sample-usage.json
sample-prepaid-credits.json

# Raw endpoint captures from tools/capture_usage.py (diff against the samples)
*-capture.json

# Generated code-review output
reviews/


# Poller state: the rotated session cookie (a live credential) and the cached
# org id. Written next to the sample DB (see DB_PATH).
session-cookie.json
org-id
